# Local AI Models

When `SYSTEM_EMBEDDING_MODEL_OPTION=local`, the shared embedding client downloads
and reuses local embedding models from this folder.

Default local embedding model:

- `all-MiniLM-L6-v2`
