interface:
  display_name: "Report Jurisdigta Bug"
  short_description: "Report bugs, then offer approved implementation"
  default_prompt: "Use $report-jurisdigta-bug to investigate a defect, create an approved GitHub issue, and ask separately before implementation."

dependencies:
  tools:
    - type: "mcp"
      value: "github"
      description: "Read repository context and create confirmed GitHub issues."
      transport: "streamable_http"
      url: "https://api.githubcopilot.com/mcp/"
