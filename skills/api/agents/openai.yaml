interface:
  display_name: "Start API"
  short_description: "Start API on 8080 with postgres + azurefoundry"
  default_prompt: "Use $api to start juris-api defaults (local postgres + azurefoundry) on port 8080."
